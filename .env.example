# Copy this file to `.env` and fill in your values.
# `.env` is gitignored — do not commit it.

# Novant API key — used to provision the dev Novant data source.
# Get one from https://novant.io
NOVANT_API_KEY=ak_your_key_here
